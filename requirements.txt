# ==========================================================================
# Reservoir Surrogate Assessment — pinned Python dependencies
# Tested on: Ubuntu 24.04 (WSL2), Python 3.10, CUDA 12.1, NVIDIA GPU
#
# IMPORTANT (Linux): the default PyPI torch==2.4.0 wheel IS the cu121 build.
# IMPORTANT (Windows-native): NOT supported by PhysicsNeMo — use WSL2. If you
#   must use Windows directly, the PyPI torch==2.4.0 wheel is CPU-only; you'd
#   need:  pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121
# ==========================================================================

# PyTorch Geometric extension wheels (compiled for torch 2.4.0 + cu121)
--find-links https://data.pyg.org/whl/torch-2.4.0+cu121.html

# --- Core DL stack (pinned: PyG extensions are ABI-locked to this torch) ---
torch==2.4.0
torchaudio==2.4.0
torchvision==0.19.0
numpy>=1.26.0,<2.0

# --- Config / experiment management ---
hydra-core>=1.3.0
omegaconf>=2.3.0
mlflow>=3.4.0
termcolor>=2.0.0

# --- PyTorch Geometric + compiled extensions ---
torch-geometric>=2.6.0
torch-scatter>=2.1.2
torch-sparse>=0.6.18
torch-cluster>=1.6.3
torch-spline-conv>=1.2.2

# --- Data handling ---
h5py>=3.14.0

# --- NVIDIA PhysicsNeMo framework (provides DistributedManager, MeshGraphNet,
#     LaunchLogger, checkpoint utils). Pinned to the version the patches target. ---
nvidia-physicsnemo==1.3.0

# --- PhysicsNeMo 1.3.0 transitive deps NOT auto-pulled by --no-deps installs.
#     If you install physicsnemo with full deps you can drop these; we list them
#     because the clean install path uses --no-deps to protect the torch pin. ---
s3fs
einops
onnx
timm
treelib
xarray
zarr
